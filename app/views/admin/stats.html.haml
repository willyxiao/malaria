%table
    %tbody
        %tr
            %td Games started
            %td= @games.length
        %tr
            %td Users registered
            %td= @users.length
        %tr
            %td Number of facts seen
            %td= @views.length
= render partial: 'users', locals: { users: @users }